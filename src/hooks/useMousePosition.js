// Convenience re-export so components can `import useMousePosition`.
// Returns { pointer, mvX, mvY, isTouch } from the global MouseProvider.
export { useMouse as default } from '../lib/MouseProvider.jsx'
